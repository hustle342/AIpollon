[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "aipollon"
version = "0.1.0"
description = "CLI-first Windows repair agent that synthesizes MCP tools"
authors = [ { name = "AIPOLLON" } ]
readme = "README.md"
requires-python = ">=3.10"
